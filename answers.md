1. it takes the front page from reddit and filters it into a combined feed.
2. it means the AI is not to blame because there is not enough context for it to base its curation off of.
3. The purpose of file A is to document the data for the curator to read. It contains the data
4. File B filters out unneeded posts using a set criteria. Its mostly empty so that the user can add their own criteria
5. File C weights the different posts to see what is a good post or bad post. File A and B don't weight the documents themselves, they give criteria
6. File B, you would tell it to not include posts containing sports content
7. fetch_reddit.py scrapes the front page of Reddit and enriches files
8. This file is a list of packages needed for the project to run, although it only contains one package
9. I would pick Youtube in order to better filter through my home page to videos I would actually like
10. I would still use file A to get the front page posts, file B would contain the different requirments I have for a post to get through the curator, file C would stay largely the same like file A.